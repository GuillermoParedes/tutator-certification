import { LivingDirective } from './living.directive';

describe('LivingDirective', () => {
  it('should create an instance', () => {
    const directive = new LivingDirective();
    expect(directive).toBeTruthy();
  });
});
